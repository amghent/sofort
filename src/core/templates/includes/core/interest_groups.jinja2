{% macro create_ig_boxes() %}
    {% for ig in interest_groups %}
        {{ card_factory.create_action_card(ig.name, ig.description, url('interest_group_index', args = [ig.slug]),
            "Go to "+  ig.name + "'s home") }}
    {% endfor %}
{% endmacro %}

{{ card_factory.create_card_box(create_ig_boxes()) }}
