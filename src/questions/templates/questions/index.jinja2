{% extends "base.jinja2" %}

{% block sidebar %}
    {% include "includes/interests/sidebar.jinja2" %}
{% endblock %}

{% block content %}
    <div id="content-wrapper" class="container-fluid">
        <div>
            <a href="{{ url('questions_new', args=[interest_group.slug]) }}"
               class="btn btn-primary float-end" id="new_question">New Question</a>
        </div>

        <div>
            <h1>Questions</h1>    

            <table id="questions_dt" class="table table-striped w-100 border border-1">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Author</th>
                        <th>Created</th>
                    </tr>
                </thead>
                <tbody>
                    {% for q in questions %}
                        <tr>
                            <td>
                                <a href="{{ url("questions_detail", args=[interest_group.slug, q.id]) }}">
                                    {{ q.title }}
                                </a>
                            </td>
                            <td>{{ q.author }}</td>
                            <td>{{ q.created_at | dt_to_user_frmt }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

{% block datatable_scripting %}
    <script type="text/javascript">
        $(document).ready(function () {
            $('#questions_dt').DataTable();
        });
    </script>
{% endblock %}
